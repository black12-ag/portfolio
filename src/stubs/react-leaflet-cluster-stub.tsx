import React from 'react';

const MarkerClusterGroup: React.FC<{ children?: React.ReactNode } & Record<string, unknown>> = ({ children }) => (
  <>{children}</>
);

export default MarkerClusterGroup;


